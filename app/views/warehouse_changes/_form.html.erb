<%= form_with(model: warehouse_change, local: true) do |form| %>
  <% if warehouse_change.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(warehouse_change.errors.count, "error") %> prohibited this warehouse_change from being saved:</h2>

      <ul>
      <% warehouse_change.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label 'product_id' %>
    <%= form.collection_select(:product_id, products_list, :id, :name) %>
  </div>

  <div class="field">
    <%= form.label :reason_id %>
    <%= form.collection_select(:reason_id, reasons_list, :id, :name) %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
